# SpendZero — Mobile (Flutter)

Folder structure placeholder matching `../docs/06-flutter-structure.md`.
Only `app/`, `core/theme`, and `features/onboarding` (splash screen) are
implemented so far — see `../docs/12-mvp-scope.md` for build order.
